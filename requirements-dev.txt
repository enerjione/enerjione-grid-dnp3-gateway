-r requirements.txt

pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0
ruff>=0.5,<1.0
# test_deployment_contract.py uretim compose sablonunu PARSE EDEREK dogruluyor;
# bildirilmedigi icin CI'da collection ImportError veriyordu ve TEK BIR TEST
# BILE KOSMUYORDU (Interrupted: 1 error during collection).
pyyaml>=6.0,<7.0
